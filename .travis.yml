language: node_js
node_js: "12.10.0"

cache:
  npm: false

env:
  global:
    - DEBUG=off
    - NODE_ENV=production

os:
  - osx

before_install:
  - pwd
  - cd gacha

install:
  - pwd
  - npm install

script:
  - pwd
  - npm test
  - npm run dist -- -mwl

deploy:
  provider: releases
  token: $GITHUB_PERSONAL_TOKEN
  file:
    - release/*.exe
    - release/*.dmg
    - release/*.deb
  cleanup: false
  on:
    tags: true

